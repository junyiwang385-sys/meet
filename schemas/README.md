# Schema 目录

当前 Schema 按业务边界分组；根目录下的旧平铺文件保留为兼容来源。

- `meeting/`：章节和会议结构。
- `transcript/`：转写段/turn。
- `summary/`：纪要摘要。
- `result/`：Harness 结果和 Gateway 诊断投影。
- `observability/`：run manifest、事件、metrics 和 error report sidecar。
