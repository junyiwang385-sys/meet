# 测试与测评覆盖分析

> 梳理全链路每一步「用什么方法」并核对「代码测试（单元/契约/集成）」与「质量测评」的覆盖，标出缺口。交接用；截至 2026-09-12（以代码为准）。
> **一句话结论**：质量测评（摘要/分章/ASR/VAD/热词，量化 eval）比代码测试扎实；薄弱在**确定性代码单测**（`validation`/`postprocess`/`gateway` 上传）与**长会 + 自动化 e2e 回归**。

## 一、现有代码测试清单（`tests/`）

`python -m pytest tests` 运行。unittest 体系。

| 测试文件 | 覆盖 |
|---|---|
| `contract/test_schema_inventory.py` | schema 分组存在 + observability 版本与运行时常量一致 |
| `integration/test_extracted_sidecars.py` | runlog/metrics/error_report 共享 identity |
| `integration/test_p0_diagnostics.py` | 失败诊断 sidecar→worker→board→gateway 全程不泄漏 prompt/content |
| `unit/adapters/board/test_worker_sidecars.py` | Board Worker 读 canonical sidecar、bounded 投影、`diagnostic_source` |
| `unit/adapters/gateway/test_formal_export.py` | 正式版 HTML/TXT/JSON 渲染 + manifest |
| `unit/harness/test_package_import.py` | 版本导出、`PipelineStageError.return_code` 契约 |
| `unit/llm/test_chunking.py` | token 预算估算确定性 |
| `unit/llm/test_request_logging.py` | split 失败/未闭合 think → bounded error |
| `unit/observability/test_runlog.py` · `test_run_report.py` | 事件 JSONL/manifest/error/metrics；报告分节/校准/红旗 |
| `unit/stages/test_topic_segmentation.py` | 分章 A 层（VAD gap/换人/深谷/软目标/确定性，11 例） |
| `unit/stages/test_product_summary_blocks.py` | 块摘要校验/过短重试/refs 回退/reduce/overview 来源/think |
| `unit/stages/test_speaker_and_display.py` | 发言人文档/refs 代码指派/unknown 顺延 |
| `unit/stages/test_meeting_minutes_map_reduce_v0.py` · `test_meeting_overview_map_reduce_v0.py` | 板端旧独立 minutes/overview map-reduce |

## 二、现有质量测评清单（`eval/`）

| 目录/脚本 | 评什么 | 指标 |
|---|---|---|
| `eval/golden_v2/`（`score_candidate`/`pc_*_recall`/`pc_hotword_ab`/`pc_keyword_*`/`pc_denoise_*`/`salvage_*`） | 摘要质量、对标飞书/通义、热词、数字打捞、去噪 | 核心事实召回、**over_decision=0**、忠实度、B-WER/U-WER |
| `eval/transcription/`（`run_point1_vad`/`run_point2_diar`/`run_point3_asr`/`run_point4_e2e`/`run_vad_sweep`/`run_p1_full30`） | VAD/分离/ASR/端到端 | FA·信息丢失 / DER·纯度 / CER·B-WER / 端到端 CER |
| `eval/topic_segmentation/cohesion_boundary_recall.py` | 分章边界 | Pk/WindowDiff/边界召回 |
| `eval/finetune/`（`build_train_pairs`/`train_qlora`/`eval_finetune`） | 4B 微调 held-out | 召回（负结果） |
| `eval/summary/keypoint_recall.py` · `eval/metrics/`（deterministic/geval/keypoint） | 关键点召回、确定性/生成式指标 | — |
| `eval/judge/`（`qwen_judge`/`calibrate`） | LLM 判官 | — |
| 实验：`a0_vs_a1_production` / `batch_summary_experiment` / `chunk_size_experiment` / `context_experiment` / `bilateral_context_experiment` | 块大小/上下文/carryover 等设计取舍 | — |

## 三、链路方法 × 覆盖矩阵

| 步骤 | 方法 | 代码测试 | 质量测评 | 缺口 |
|---|---|---|---|---|
| 音频标准化 | SoX mono/16k | — | — | 外部工具，低 |
| 前端 VAD | fsmn-vad `thres=0.2` | — | ✅ 点1（n3 + 全30场 sweep） | 测评足；板端脚本无单测 |
| 说话人分离 | 3D-Speaker/CAM++ win3/thr0.68 | — | ✅ 点2（PC + 板端 rttm） | segment 脚本（unknown 吸收）**无当前单测**（旧仓有） |
| Batch ASR | Qwen3-ASR 常驻 runner | — | ✅ 点3 + 热词 A/B | 编译件；编排脚本无单测 |
| 转写规范化 `transcript` | canonical seg/ms/speaker 归一 | ❌ | 间接 | **无单测** |
| 后处理 `postprocess` | 重叠去重(LCS) + 专名纠错 | ❌ | 专名→点3 B-WER；去重无 | **无单测**（确定性关键逻辑） |
| 分章 `topic_segmentation` | VAD gap/换人/内聚谷深度 | ✅ 11 例 | ✅ Pk + cohesion_boundary_recall | eval **仅 1-few 场**，cue 泛化未验 |
| 块摘要 map `product_summary` | 受控抽取 A0 | ✅ | ✅ golden_v2 + a0/chunk_size | 足 |
| reduce/概览 | 只合已校验章节 | ✅ | ✅ golden_v2 | 足 |
| 板端 minutes/overview | 旧独立 map-reduce | ✅ 各 1 | — | 足 |
| 发言人 batches | 按 speaker 聚类 | ✅ | ❌ 无专项质量评测 | 发言人纪要质量无 eval |
| 行动项 action-review | 明确指派才升格 | 部分 | decisions:over_decision=0；owner/deadline 弱 | action owner/deadline 质量评测缺 |
| `enrichment` 关键词/问答/金句/决策/大纲 | 受控生成 + snap 保真 | ❌ | 关键词/decisions 有；qa/quotes/outline 无 | **无单测** + qa/quotes/outline 无 eval |
| 校验发布 `validation` | refs 锚/owner 证据/边界修复 | ❌ | 间接 | **无单测（核心！）** |
| 兼容导出 `compat_export` | 确定性投影 | ❌（旧仓有） | — | **无单测** |
| 展示 `display` | 前端视图/unknown 顺延 | ✅（部分） | — | 部分 |
| LLM session `llm.py` | rkllm3-server 生命周期 | 部分 | run_report token 经济学 | server 解析/复用无测 |
| Gateway `/api/*` | 上传/状态/草稿/正式 | 仅 formal_export + p0 集成 | — | **关键路径无测（上传 415 bug 漏测）** |
| Board Agent `/v1/*` | task_kind/单任务锁/上传 | worker_sidecars + p0 集成 | — | **API handler 无单测** |
| 可观测性/契约 | sidecar/identity | ✅ | — | 足 |
| **端到端 前端→板端→回前端** | e2e client | 手动 1 次 | ❌ 无自动回归 | **无自动化 e2e** |
| **长会 1–2h** | — | ❌ | ❌ | **完全未验（卖点）** |

## 四、缺口（按优先级）

**🔴 高（价值高 + 完全没有）**
1. **`validation.py` 单元测试**——证据校验是"不编造"底线的执行者，却无测。
2. **`postprocess.py` 单元测试**——重叠去重 LCS + 专名纠错候选/判定，确定性关键逻辑，无测。
3. **Gateway `/api` 关键路径测试**——尤其上传（content-type/大小/错误分支）；**415→假"未链接"那个 bug 就是没测漏的**（`docs/OPEN-ITEMS.md`）。
4. **长会 1–2h 端到端评测**——卖点，零验证（最大缺口，需真实长音频 + 板端时间）。

**🟠 中**
5. `transcript.py` / `compat_export.py` / `enrichment.py` 补单测。
6. Board Agent API 测试（task_kind/单任务锁/上传校验）。
7. 分章 eval 扩到 **3–5 场**（cue 跨会泛化，现仅 1-few 场）。
8. 发言人纪要 & action owner/deadline 质量评测。
9. **端到端产品链路自动化回归**（把 `ops/e2e/gateway_e2e_client.py` 包成可复跑评测 + 断言结果结构）。

**🟡 低/可选**
10. SoX/VAD/ASR runner（外部/编译件，难单测）；`llm.py` session（需 mock server）。

## 五、怎么补 / 怎么跑

- 代码测试：新增到 `tests/unit/stages/`（validation/postprocess/transcript/compat_export/enrichment）、`tests/unit/adapters/gateway/`（api 上传路径）、`tests/unit/adapters/board/`（agent api）。跑 `python -m pytest tests`。
- 质量测评：新增到对应 `eval/` 子目录；分章扩场改 `eval/topic_segmentation/`；发言人/action 质量可挂到 `eval/golden_v2/score_candidate` 的维度。
- e2e 自动化回归：以 `ops/e2e/gateway_e2e_client.py` 为基础，加结果结构断言（阶段序列到 `meeting_ready`、`result` 含 summary/chapters/speakers）。
- 长会：需真实 1–2h 音频 + 板端排期；先在 `eval/transcription/`、`eval/golden_v2/` 搭长会样本框架，再实测。

> 诚实边界：本矩阵按当前 `tests/`、`eval/` 实际文件核对；"质量测评"多为 PC 等价替代 + 单/少场（n=3 或 30 场，非大规模 benchmark），绝对值板端会有差异；长会未验。
