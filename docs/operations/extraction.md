# Project extraction operations

The new `src/meeting_agent` package is a parallel extraction of the latest local code. The existing `scripts/` and `board_sync_20260820/` paths remain the compatibility and deployment sources until a separate cutover is approved.

Do not treat this extraction as proof of a successful RK1828 deployment. Board execution, Gateway startup, real meeting reprocessing, and production readiness require separate verification.
