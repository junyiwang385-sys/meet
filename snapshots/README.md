# Deployment snapshots

`board_sync_20260820/` is a preserved copy of the local RK1828 deployment snapshot. Its `SHA256SUMS` excludes reproducible `__pycache__` files. The snapshot is read-only reference material; the extracted package under `src/` is the new local source layout.
