# CLAUDE.md — meet（端侧会议纪要）项目约束

端侧会议纪要系统：RK1828 NPU + 量化 Qwen3-4B（W4A16, ctx=8k）+ Qwen3-ASR-0.6B + 3D-Speaker。
目标：1-2h 长会离线纪要，主打隐私/离线。

本文件是**长期约束**，改动本项目（尤其摘要/纪要环节）前务必遵守以下"小模型能力边界"与"设计原则"。

---

## 一、核心模型是"小模型"（4B），先认清它的能力边界

以下边界经 PC 实测（Ollama qwen3:4b GGUF Q4）+ 论文佐证得到。**设计必须围绕这些边界，不要假设 4B 有大模型的能力。**

### ✅ 4B 强项（可放心依赖）
- **检索**：从长文找显式标记信息——实测 RULER-lite 8k 内任意长度/位置 100%，无 lost-in-middle。
- **聚合**：找齐多个显式项——多针检索 100%。
- **二分类/判断**：是否/分类/成词性判断——稳。
- **受控抽取/填空**：schema 引导逐项抽取——稳（接近检索水平）。

### ⚠️ 4B 弱项（≤30%，不要依赖）
- **自主归纳**：从长文自己判断"什么重要"并压缩——实测一次多话题归纳召回 ≤30%，会丢负责人、张冠李戴。
- **自主判断重要性**：从口语自主抽要点——召回 20-40%。
- **长程篇章理解**：议程级分章——平滑议题会仅命中标杆 43%。
- **精确低频信息**：只说一次的数字/专名易漏（如财务"80万/70万"）。

### ❌ 4B 明确做不到
- 徒手输出离散决策（自己定章节切点 → 边界"语义任意"，输入变一句就漂）。
- 一次多任务复合结构化（同时分章+摘要+归因+抽待办 → 长会崩）。
- 深层跨块推理。

### 关键机理（决定设计）
- **瓶颈不是上下文长度，是"语义判断+归纳"**——检索满分但归纳弱。
- **但长度会独立加剧归纳变差**：EMNLP 2025 Findings《Context Length Alone Hurts LLM Performance Despite Perfect Retrieval》实测——即使检索完美、无干扰，**光输入变长，归纳类任务就断崖下降**（VarSum 30k tokens 降 85%，且**下降大部分发生在 7k 以内**）。→ 块越短归纳质量越高。
- **小模型偏保守，病在"漏"不在"编"**：幻觉相对大模型可能更少（Pruned-LLM 研究），但**覆盖不全/归纳丢信息更严重**。防护重点是**补召回**，不是主要防幻觉（但顺滑/自由生成仍会造字，见下）。
- **≥3B 才能利用"脚手架"**：<3B 用检索/提示脚手架常负收益（Big Reasoning with Small Models）。4B 恰在门槛之上——所以 carryover/主题词/schema 对 4B 有效，选 4B 不选更小是踩这个门槛。

---

## 二、设计原则（8 条，违反会踩已知的坑）

1. **智能放确定性 DAG，不放模型自主决策**。切分/去重/边界用算法，4B 只做块内摘要/判断。
   （4B 徒手切章节"语义任意"，改确定性 A 层分块后才稳。）

2. **把"自主归纳"降级成"受控抽取"**（extract-then-compose）。别让 4B "读一大段自己判断重要性"，改 schema 引导"先抽要点再成文"。
   （自主归纳 ≤30%，受控抽取接近检索水平；加厚章节 overview 用抽取式才不注水。）

3. **一次一任务，不复合**。一次调用只做一件事。
   （单请求全功能长会崩；多话题一起归纳 ≤30%。）

4. **carryover 用"压缩摘要"，绝不给前文原文**。跨块衔接给上块一句话摘要。
   （实测：喂前文原文串味严重、当前摘要被带偏；喂压缩摘要聚焦、串味砍半。）

5. **全局理解靠"显式构造"，不靠模型通读**。长会用主题词表+会议设定给全局锚点，reduce 补关联。**但短会（能塞进 8k）直接单请求更好**（实测短会一次通读反超两遍式）——按会议长度分流（pipeline 的 `use_single_request` 快路径正是此意）。

6. **强 system 约束 + 规则过滤，不用 few-shot**。
   （弱 4B 对 few-shot 极敏感、会 echo 示例内容——踩坑三次：问答 echo "餐饮部/预算十万"、纠错 echo "明确待办"。改强约束+规则后好。）

7. **结构化输出（JSON schema 约束）**，带 reason 字段让它先推理。
   （4B 自由生成会改标点、复述 prompt、造字如"蚯和粉"。）

8. **分层：无损前置喂下游，有损后置只展示；证据锚原文**。
   去重/专名纠错（无损）→ 喂摘要/抽取/证据链；顺滑（有损）→ 仅展示。**refs 锚 L1 无损原文，不锚 L2**。
   （4B 顺滑造字"蚯和粉"——有损内容进证据链会污染凭证。）

---

## 三、一句话总纲

**用 4B = 把大模型靠"大参数+长上下文"隐式完成的事，拆成小模型能扛的确定性步骤显式完成。**
确定性算法搭骨架（切分/去重/边界）→ 4B 只做它强的（检索/抽取/判断/填空）→ 避开它弱的（自主归纳/篇章理解/复合任务）→ 全局显式构造（主题词/carryover 压缩摘要/reduce）→ 输出强约束（schema+规则，不用 few-shot）→ 分层隔离（无损喂下游/有损只展示/证据锚原文）。

---

## 四、诚实边界（写代码/评估时须知）

- 以上实测均为 **PC 等价替代**（Ollama GGUF Q4 / transformers Qwen3-ASR / modelscope diar），**非板端 RKLLM/RKNN/板端 3D-Speaker 复现**。原则是方法论层面可信，绝对数值板端会不同。
- 多为**单/少场会**，非大规模 benchmark；论文佐证是"方向一致"（RULER=COLM'24、Context-Length=EMNLP'25 Findings 为顶会评审；其余部分为 arXiv，发表状态未逐一核）。
- **关键未验证**：1-2h 长会（项目卖点）、板端端到端——尚未真跑。"结构补参数"的价值在长会才充分体现，须补长会验证。
- 面试/汇报口径：讲"架构 + PC 验证 + 能力边界认知"，**不夸"端侧跑通长会"**（未测）；实验非实习交付，不冒充成果。
